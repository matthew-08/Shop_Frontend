overwrite: true
schema: 'http://localhost:4000'
documents: './src/graphql/'
generates:
  src/generated/gql/:
    preset: 'client'
    plugins: ['typescript-urql']
  ./graphql.schema.json:
    plugins:
      - 'introspection'
