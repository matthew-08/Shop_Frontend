input LoginInput {
  email: String!
  password: String!
}

mutation LogIn($LoginType: LoginType!) {
  login(input: $LoginType) {
    __typename
    ... on MutationLoginSuccess {
      data {
        email
        id
        token
      }
    }
    ... on Error {
      message
    }
  }
}
